{{#each @items as |item|}}
  {{yield (hash
    item=item
    checkbox=(component 'rose/form/checkbox'
      name=@name
      disabled=@disabled
      inline=@inline
      defaultFilters=@defaultItems
      checked=(includes item @selectedItems)
      onChange=(fn this.toggleItem item @defaultItems)
    )
  )}}
{{/each}}
